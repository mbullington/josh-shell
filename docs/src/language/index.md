# Language guide

The guide explains Josh's model from lexical mode through values, functions, control flow, process boundaries, structured streams, files, and startup configuration. Jobs and modules remain explicitly unavailable.

Josh is intentionally not JavaScript or POSIX shell. Do not infer a feature from visual similarity. Use the exact status block, exclusions, and reference tables.
