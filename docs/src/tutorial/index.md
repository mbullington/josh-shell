# Josh tutorial

<div class="status-coverage">

**Status coverage:** This page makes no product-availability claims. Tutorial examples link to atomic statuses and the [capability matrix](../status/matrix.md).

</div>

This tutorial follows the current vertical slice. It starts at the interactive prompt, separates commands from expressions, moves values into argv, captures output, builds pipelines, and ends with a script.

Only examples labeled **Runnable example · Implemented** are instructions. Later language chapters show specified or planned forms for context, but the tutorial does not ask you to run unavailable syntax.

Before starting, build Josh using [Install and build](../welcome/install.md). Commands use common Unix programs such as `printf`, `tr`, and `sh`.
