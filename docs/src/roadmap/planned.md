# Planned work

## J-JOBS-001

Background statements and terminal job control require process-group transfer, status ownership, signal routing, cleanup, a job table, and Job values. Job-value assignment syntax remains [Unresolved](unresolved.md#J-BG-001). Current `&`, `jobs`, `fg`, and `bg` forms are rejected; no partial background execution is available.

## J-MOD-001

Scoped imports/exports, general current-scope `source`, package resolution, and a standard library module resolver remain excluded. Remote modules require identity, integrity, cache, permission, update, offline, and trust policies before syntax can be accepted.

Structured streams, redirections/globs, non-job control flow, functions/UFCS, startup configuration, and deterministic PNG screenshots moved to Implemented after executable verification.
